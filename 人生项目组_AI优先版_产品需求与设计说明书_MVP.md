# 人生项目组：AI 优先版产品需求与设计说明书 MVP

> 文档版本：V0.2  
> 产品形态：微信小程序 MVP  
> 核心场景：情侣 / 朋友双人长期记录与 AI 复盘  
> 产品核心：AI 快速整理记录 + 用户确认 + 周期分析复盘  
> 扩展方向：多人小组、家庭关系、职业成长、五年人生规划系统  

---

## 1. 当前分析

### 1.1 背景

上一版产品容易滑向“打卡 + 评分 + 看板”的传统路径。这个方向能做，但不够锋利，也不够符合当前 AI 产品能力。用户真正需要的不是每天手动填写一堆字段，而是在事情发生后，可以快速把混乱的生活事件、关系冲突、职业压力、身体状态记录下来，并由 AI 自动整理成可复盘的结构化内容。

因此，本版产品逻辑调整为：

```text
用户快速输入真实事件
↓
AI 自动理解、归类、摘要、提取标签
↓
用户确认或手动微调
↓
进入个人 / 双人时间线
↓
AI 定期分析复盘
↓
发现重复模式、风险信号和下一步行动
```

产品不再以“评分打卡”为核心，而是以“AI 事件记录与复盘”为核心。评分、标签、权限、目标关联等仍然保留，但默认由 AI 预填，用户只在需要时修改。

---

### 1.2 核心痛点

| 痛点 | 具体表现 | 产品解决方式 |
|---|---|---|
| 记录成本高 | 用户不愿意手动填字段 | 支持自然语言 / 语音快速输入，AI 自动整理 |
| 情绪事件难复盘 | 吵架、压力、焦虑时表达混乱 | AI 提取事实、情绪、需求、未解决问题 |
| 长期问题反复出现 | 同一个矛盾换不同形式重复发生 | AI 周期识别重复主题和行为模式 |
| 双人关系缺少沉淀 | 重要沟通散落在聊天记录里 | 建立双人事件时间线和共同复盘 |
| 人工设置太重 | 用户不想选类型、标签、权限 | AI 先生成建议，用户确认即可 |
| AI 容易越界 | 关系、健康、财务场景敏感 | 设定 AI 能力边界，只做整理和建议，不替用户决策 |

---

### 1.3 产品目标

MVP 阶段验证一个核心假设：

> 用户是否愿意通过 AI 快速记录真实生活事件，并在一周或一个月后使用 AI 总结进行复盘。

阶段目标：

1. 用户可以用一句话或一段语音完成记录。
2. AI 可以自动生成事件标题、摘要、类型、标签和复盘问题。
3. 用户可以确认、编辑或拒绝 AI 整理结果。
4. 双人房间可以沉淀共享事件和关系复盘。
5. 情侣冲突场景可以形成从记录到冷静复盘的闭环。
6. 系统可以生成周报、月报，并识别重复模式。

---

### 1.4 成功标准

MVP 上线 4 周内，满足以下条件可判断方向成立：

| 指标 | 目标 |
|---|---|
| 首次记录完成率 | ≥ 65% |
| AI 整理结果确认率 | ≥ 60% |
| AI 整理结果编辑率 | ≤ 50%，若过高说明 AI 结构化质量不足 |
| 用户 7 日内再次记录率 | ≥ 30% |
| 双人房间创建率 | ≥ 20% |
| 冲突复盘功能使用率 | 情侣房间用户中 ≥ 25% |
| 周报生成率 | 活跃用户中 ≥ 30% |
| 周报保存 / 分享率 | ≥ 40% |

---

## 2. 产品定位

### 2.1 产品名称

暂定：**人生项目组**

### 2.2 一句话定义

> 人生项目组是一款面向情侣和朋友的 AI 长期记录与复盘工具，帮助用户快速记录重要事件，并通过 AI 自动整理、总结和识别关系与人生目标中的重复模式。

### 2.3 产品不是

| 不是 | 原因 |
|---|---|
| 不是普通打卡软件 | 核心不是完成次数，而是真实事件与复盘 |
| 不是情侣审判工具 | AI 不判断谁对谁错 |
| 不是心理咨询产品 | AI 不提供诊断或治疗 |
| 不是理财工具 | 只记录财务目标，不给投资建议 |
| 不是日记软件换皮 | 重点在 AI 结构化、双人协作和周期复盘 |

### 2.4 产品原则

1. **AI 先整理，人再确认**：默认不让用户手动填复杂字段。
2. **记录真实事件，而不是表演自律**：不做排行榜，不强化连续天数。
3. **复盘优先于打卡**：记录只是输入，复盘才是价值输出。
4. **隐私默认保护**：原始记录默认私密，共享必须由用户确认。
5. **中立不站队**：尤其在情侣冲突场景，AI 只整理事实、情绪和需求。
6. **保留人工控制权**：所有 AI 结果都可编辑、删除、重新生成。

---

## 3. 目标用户与首个场景

### 3.1 目标用户

#### A. 情侣 / 异地情侣

核心需求：

- 快速记录每次重要沟通、冲突和计划。
- 复盘反复出现的问题。
- 识别关系中的高频矛盾主题。
- 在冷静后生成更好的表达方式。
- 沉淀共同决定，例如见面计划、城市选择、长期规划。

#### B. 朋友 / 成长搭子

核心需求：

- 记录学习、职业、健康、财务目标推进。
- 让 AI 定期总结进展和停滞点。
- 互相共享关键事件，而不是每天互相催打卡。

### 3.2 MVP 首个强场景

推荐第一版重点做：**情侣关系事件记录与冲突复盘**。

原因：

1. 痛点强，记录意愿高。
2. AI 自动整理价值明显。
3. 双人房间有天然留存。
4. 复盘结果容易被用户感知。
5. 可自然扩展到朋友、家庭、成长小组。

---

## 4. 核心业务闭环

### 4.1 通用事件记录闭环

```text
打开小程序
↓
输入一句话 / 一段语音
↓
AI 自动整理为结构化事件
↓
用户确认 / 修改
↓
选择可见范围
↓
事件进入时间线
↓
AI 每周 / 每月总结
↓
用户确认下一步行动
```

### 4.2 情侣冲突复盘闭环

```text
发生争执
↓
一方点击“记录一次冲突”
↓
输入原始描述或语音
↓
AI 整理事实、情绪、冲突主题、可能需求
↓
用户确认，默认私密保存
↓
系统建议冷静后复盘
↓
用户选择是否共享摘要 / 邀请对方复盘
↓
双方分别补充自己的视角
↓
AI 生成中立复盘
↓
双方确认一个下次行动约定
↓
进入关系时间线
```

---

## 5. 信息架构

### 5.1 底部导航

MVP 建议 4 个 Tab：

```text
首页
记录
房间
我的
```

### 5.2 页面结构

```text
人生项目组
│
├── 登录 / 新手引导
│
├── 首页
│   ├── 快速记录输入框
│   ├── 场景快捷入口
│   ├── AI 待确认记录
│   ├── 最近事件时间线
│   ├── 本周复盘提醒
│   └── 双人房间动态
│
├── 记录
│   ├── 新建记录
│   ├── AI 整理确认页
│   ├── 事件详情页
│   ├── 历史时间线
│   └── 筛选与搜索
│
├── 房间
│   ├── 房间列表
│   ├── 房间详情
│   ├── 共同事件时间线
│   ├── 冲突复盘
│   ├── 共同决定
│   └── 房间周报
│
└── 我的
    ├── 个人资料
    ├── AI 设置
    ├── 隐私设置
    ├── 提醒设置
    ├── 数据导出
    └── 场景模板管理
```

---

## 6. 核心功能需求

## 6.1 快速记录

### 6.1.1 功能描述

用户通过自然语言或语音快速记录一个真实事件。系统不要求用户先选择复杂字段，而是由 AI 自动识别事件类型、主题、情绪、人物、是否关联房间、是否需要复盘。

### 6.1.2 输入方式

| 输入方式 | MVP 是否支持 | 说明 |
|---|---|---|
| 文字输入 | 是 | 首页主入口 |
| 场景快捷按钮 | 是 | 冲突、沟通、压力、决定、开心时刻 |
| 语音输入 | P1 | 可先预留入口，后续接语音转文字 |
| 图片 / 聊天截图 | P2 | 涉及隐私和 OCR，暂不做 |

### 6.1.3 快捷场景

首页提供以下按钮：

```text
记录一次冲突
记录一次沟通
记录一个开心时刻
记录一个压力事件
记录一个重要决定
随便记一笔
```

### 6.1.4 用户故事

作为用户，我希望在情绪复杂或事情刚发生时，用一句话快速记录，而不是先思考该选哪个分类、打几分、填哪个表。

### 6.1.5 验收标准

- 用户可以输入一段自然语言并保存。
- 保存后系统自动调用 AI 整理。
- AI 返回标题、摘要、事件类型、标签、建议权限、是否需要复盘。
- 用户可确认、编辑、重新生成或丢弃 AI 结果。
- 用户确认后，事件进入个人时间线或房间时间线。

---

## 6.2 AI 自动整理

### 6.2.1 功能描述

AI 对用户原始输入进行结构化处理，生成一条可保存、可检索、可复盘的事件记录。

### 6.2.2 AI 输出字段

| 字段 | 说明 | 是否允许用户修改 |
|---|---|---|
| ai_title | AI 生成标题 | 是 |
| ai_summary | 事件摘要 | 是 |
| event_type | 事件类型 | 是 |
| topic_tags | 主题标签 | 是 |
| emotion_tags | 情绪标签 | 是 |
| need_tags | 需求标签 | 是 |
| people_involved | 涉及人物 | 是 |
| suggested_visibility | 建议可见范围 | 是 |
| review_required | 是否建议复盘 | 是 |
| follow_up_questions | 后续复盘问题 | 可删除 |
| safety_flags | 安全风险标记 | 否，只能系统处理 |

### 6.2.3 事件类型

| event_type | 中文名 | 示例 |
|---|---|---|
| conflict | 冲突 / 争执 | 因见面计划吵架 |
| communication | 深度沟通 | 讨论未来城市 |
| positive_moment | 积极时刻 | 对方今天很支持我 |
| stress | 压力事件 | 夜班后非常疲惫 |
| decision | 重要决定 | 决定春节见双方父母 |
| plan | 计划安排 | 下个月见面 |
| health | 健康状态 | 连续失眠 |
| career | 工作 / 学习 | 工作适应、项目进展 |
| finance | 财务事件 | 储蓄、消费、预算 |
| self_reflection | 自我反思 | 觉得自己变麻木 |
| other | 其他 | AI 无法判断时使用 |

### 6.2.4 AI 整理示例

用户输入：

```text
今天又因为见面时间和她吵架了。她觉得我一直不主动规划，我觉得我最近夜班太累，不是不想见，只是脑子真的转不动。
```

AI 输出：

```json
{
  "ai_title": "因见面计划产生争执",
  "event_type": "conflict",
  "ai_summary": "本次冲突围绕下次见面时间展开。对方认为你在见面规划上不够主动，你认为近期夜班导致精力不足，并非不重视关系。",
  "topic_tags": ["见面计划", "异地关系", "工作疲劳", "主动性期待"],
  "emotion_tags": ["疲惫", "委屈", "防御"],
  "need_tags": ["被理解", "明确计划", "关系安全感"],
  "suggested_visibility": "private",
  "review_required": true,
  "follow_up_questions": [
    "你真正想让对方理解的是什么？",
    "对方真正需要的是明确计划，还是更多主动表达？",
    "是否可以在休息日固定讨论见面安排？"
  ]
}
```

---

## 6.3 人工确认机制

### 6.3.1 功能描述

AI 整理结果不会直接入库成为最终记录，必须经过用户确认。用户可以快速确认，也可以展开高级设置进行修改。

### 6.3.2 确认页结构

```text
AI 已帮你整理
│
├── 标题
├── 摘要
├── 类型
├── 标签
├── 建议可见范围
├── 是否需要复盘
├── 后续问题
└── 操作按钮
    ├── 确认保存
    ├── 手动修改
    ├── 重新整理
    └── 丢弃
```

### 6.3.3 默认交互

- 默认展示简洁版，只显示标题、摘要、类型和权限。
- 标签、情绪、需求等字段折叠展示。
- 用户点击“确认保存”即可完成。
- 用户点击“手动修改”进入完整编辑。

### 6.3.4 验收标准

- AI 整理结果必须可编辑。
- 用户未确认前，不进入正式时间线。
- 用户可保存原始输入，不采用 AI 整理。
- 用户可对 AI 结果进行反馈：准确 / 不准确。

---

## 6.4 冲突复盘功能

### 6.4.1 功能定位

冲突复盘是 MVP 的核心场景功能。它不是“吵架证据库”，而是帮助双方在冷静后理解：

1. 这次争执的事实是什么？
2. 双方各自的情绪是什么？
3. 背后未被满足的需求是什么？
4. 这个问题是否反复出现？
5. 下次如何避免升级？

### 6.4.2 入口

- 首页快捷按钮：记录一次冲突。
- 房间详情页：发起冲突复盘。
- 事件详情页：将某条事件转为冲突复盘。

### 6.4.3 冲突记录阶段

用户输入原始描述后，AI 自动整理：

| 输出项 | 说明 |
|---|---|
| 争执主题 | 例如见面计划、回复消息、未来城市、金钱消费 |
| 事实摘要 | 尽量只描述发生了什么 |
| 用户情绪 | 从输入中提取，不做绝对判断 |
| 对方可能情绪 | 使用“可能”表达，避免定性 |
| 双方需求 | 例如安全感、理解、空间、明确计划 |
| 待确认问题 | 需要后续沟通的问题 |
| 复盘建议时间 | 例如 24 小时后 |

### 6.4.4 冷静复盘阶段

系统在指定时间提醒用户进行复盘。复盘问题：

```text
1. 如果只描述事实，这次发生了什么？
2. 我当时最强烈的感受是什么？
3. 我真正想表达的需求是什么？
4. 我觉得对方可能在意什么？
5. 我有没有把疲惫、压力或不安转化成攻击？
6. 下次遇到类似情况，我们可以提前约定什么？
```

### 6.4.5 双人复盘

用户可以选择邀请对方参与。双方分别填写自己的视角后，AI 生成中立共同复盘。

共同复盘输出结构：

```markdown
# 本次冲突复盘

## 共同事实

## 双方主要感受

## 可能的底层需求

## 争执升级点

## 重复出现的模式

## 下次可以尝试的约定
```

### 6.4.6 安全边界

AI 禁止输出：

- “你是对的 / 对方是错的”。
- “你应该分手”。
- “对方就是不爱你”。
- “这是冷暴力 / PUA / 操控”等未经充分依据的定性。

若内容涉及明确人身威胁、自伤、暴力、强迫控制等高风险信息，系统进入安全提示流程，不生成普通复盘。

---

## 6.5 事件时间线

### 6.5.1 功能描述

所有确认后的事件进入时间线，供用户回看和周期复盘。

### 6.5.2 时间线类型

| 类型 | 说明 |
|---|---|
| 个人时间线 | 只展示自己的所有事件 |
| 房间时间线 | 展示双方共享事件 |
| 冲突时间线 | 筛选 conflict 类型 |
| 决定时间线 | 筛选 decision / plan 类型 |
| 积极时刻时间线 | 筛选 positive_moment 类型 |

### 6.5.3 时间线卡片字段

```text
事件标题
事件摘要
事件类型
标签
发生时间
可见范围
是否已复盘
AI 是否发现重复模式
```

---

## 6.6 双人房间

### 6.6.1 功能描述

用户可以创建双人房间，用于与伴侣或朋友共享部分事件、共同复盘和沉淀共同决定。

### 6.6.2 房间类型

| 类型 | 说明 |
|---|---|
| couple | 情侣长期记录 |
| friend | 朋友成长搭子 |
| custom | 自定义 |

### 6.6.3 房间详情页

```text
房间详情
│
├── 房间名称与成员
├── 快速记录入口
├── 共同事件时间线
├── 待复盘事件
├── 共同决定
├── AI 房间周报
└── 房间设置
```

### 6.6.4 共同决定

共同决定用于沉淀双方已经达成一致的事项，例如：

- 下次见面时间范围。
- 每周固定沟通时间。
- 夜班后不讨论重大关系问题。
- 本月共同储蓄目标。
- 是否讨论未来城市选择。

共同决定字段：

| 字段 | 说明 |
|---|---|
| decision_id | 决定 ID |
| room_id | 房间 ID |
| title | 决定标题 |
| content | 决定内容 |
| source_event_id | 来源事件 |
| owner_user_id | 主要负责人，可为空 |
| due_date | 截止时间，可为空 |
| status | active / done / expired / archived |

---

## 6.7 AI 周期总结

### 6.7.1 个人周报

AI 每周基于用户确认后的事件生成个人周报。

输入：

- 本周事件列表。
- 本周事件类型分布。
- 未复盘事件。
- 上周行动建议。
- 用户确认的长期目标，可选。

输出：

```markdown
# 本周个人复盘

## 本周发生了什么

## 高频主题

## 明显变化

## 需要关注的问题

## 建议下周行动
```

### 6.7.2 房间周报

AI 基于双方授权共享的数据生成房间周报。

输出：

```markdown
# 本周共同复盘

## 本周共同事件

## 共同进展

## 冲突与沟通模式

## 未解决问题

## 建议一起确认的一件事
```

### 6.7.3 月度模式分析

每月输出更高层级的模式识别。

示例：

```text
过去 30 天中，关系冲突共记录 4 次，其中 3 次与“见面计划不明确”有关，2 次发生在工作疲劳后。系统判断：你们的主要矛盾可能不是单次沟通问题，而是计划确定性与现实精力不足之间的冲突。
```

---

## 7. AI API 能力设计

## 7.1 AI 调用原则

1. 所有 AI 输出必须基于用户提供的数据。
2. AI 输出必须结构化，便于前端展示和后端存储。
3. 关键 AI 结果必须经过用户确认。
4. AI 不直接覆盖用户原始记录。
5. AI 不使用 private 内容生成房间共享总结。
6. AI 调用需要记录 prompt_version、model_name、input_hash、output_json。

---

## 7.2 AI 能力模块

| 模块 | API 能力 | 优先级 |
|---|---|---|
| 事件理解 | 文本分类、摘要、标签提取 | P0 |
| 情绪与需求提取 | 从关系事件中提取感受和需求 | P0 |
| 权限建议 | 根据敏感度建议 private / summary_only / room_visible | P0 |
| 复盘问题生成 | 根据事件生成后续问题 | P0 |
| 冲突中立重述 | 将情绪化表达改写为中立描述 | P0 |
| 周报生成 | 对一周事件进行总结 | P1 |
| 模式识别 | 对 30 / 90 天事件做重复主题分析 | P1 |
| 沟通稿生成 | 将复盘结论转成温和表达 | P1 |
| 安全识别 | 识别自伤、暴力、威胁等风险 | P0 |

---

## 7.3 AI 结构化输出 Schema

### 7.3.1 事件整理 Schema

```json
{
  "title": "string",
  "summary": "string",
  "event_type": "conflict | communication | positive_moment | stress | decision | plan | health | career | finance | self_reflection | other",
  "topic_tags": ["string"],
  "emotion_tags": ["string"],
  "need_tags": ["string"],
  "people_involved": ["self | partner | friend | family | colleague | other"],
  "suggested_visibility": "private | summary_only | room_visible",
  "sensitivity_level": "low | medium | high",
  "review_required": true,
  "follow_up_questions": ["string"],
  "safety_flags": {
    "self_harm": false,
    "violence": false,
    "abuse": false,
    "financial_risk": false,
    "medical_risk": false
  }
}
```

### 7.3.2 冲突复盘 Schema

```json
{
  "shared_facts": ["string"],
  "user_emotions": ["string"],
  "partner_possible_emotions": ["string"],
  "user_needs": ["string"],
  "partner_possible_needs": ["string"],
  "escalation_points": ["string"],
  "unresolved_questions": ["string"],
  "suggested_agreements": ["string"],
  "neutral_reframe": "string"
}
```

---

## 7.4 Prompt 模板

### 7.4.1 事件整理 Prompt

```text
你是一个长期人生记录整理助手。请将用户输入的一段生活记录整理成结构化事件。

要求：
1. 只基于用户输入，不编造事实。
2. 不判断谁对谁错。
3. 不做医疗、心理、法律、投资结论。
4. 如果是关系冲突，请区分事实、情绪、需求和待确认问题。
5. 输出 JSON，必须符合给定 Schema。
6. 如果信息不足，请在 summary 中说明“信息不足，暂无法判断完整背景”。

用户输入：
{raw_content}

可选上下文：
{room_context}
{recent_events_summary}
```

### 7.4.2 冲突复盘 Prompt

```text
你是一个中立的关系复盘整理助手。请基于双方授权共享的信息，生成冲突复盘。

要求：
1. 不站队，不判定谁对谁错。
2. 不建议分手、冷战或惩罚对方。
3. 使用“可能”“需要确认”等措辞表达推测。
4. 输出重点是事实、情绪、需求、升级点和下次约定。
5. 不引用任何 private 内容。
6. 输出 JSON，符合冲突复盘 Schema。

输入：
{conflict_event}
{user_a_visible_view}
{user_b_visible_view}
{past_related_events_summary}
```

### 7.4.3 周报 Prompt

```text
你是一个长期人生复盘助手。请基于用户本周已确认的事件，生成一份简洁周报。

要求：
1. 只总结本周记录中出现的信息。
2. 不编造没有记录的内容。
3. 提炼高频主题、明显变化和未解决问题。
4. 最多给出 3 条下周行动建议。
5. 如果数据不足，直接说明记录不足。

输入：
{weekly_events}
{last_week_actions}
{active_goals_optional}
```

---

## 8. 数据设计

## 8.1 核心实体

```text
User 用户
│
├── LifeEvent 生活事件
├── AIExtraction AI整理结果
├── Review 复盘
└── RoomMember 房间关系

Room 房间
│
├── RoomMember 成员
├── LifeEvent 共享事件
├── ConflictReview 冲突复盘
├── SharedDecision 共同决定
└── RoomReport 房间报告
```

---

## 8.2 life_events 表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | varchar | 事件 ID |
| user_id | varchar | 创建者 |
| room_id | varchar/null | 所属房间 |
| raw_content | text | 用户原始输入 |
| event_type | enum | 事件类型 |
| title | varchar | 用户确认后的标题 |
| summary | text | 用户确认后的摘要 |
| topic_tags | jsonb | 主题标签 |
| emotion_tags | jsonb | 情绪标签 |
| need_tags | jsonb | 需求标签 |
| visibility | enum | private / summary_only / room_visible |
| event_time | datetime | 事件发生时间 |
| review_status | enum | unreviewed / reviewed / ignored |
| source_type | enum | text / voice / template |
| ai_extraction_id | varchar | 关联 AI 整理结果 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

---

## 8.3 ai_extractions 表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | varchar | AI 整理 ID |
| user_id | varchar | 用户 ID |
| raw_content_hash | varchar | 原始内容哈希 |
| prompt_version | varchar | Prompt 版本 |
| model_name | varchar | 模型名称 |
| output_json | jsonb | AI 输出 |
| user_action | enum | confirmed / edited / regenerated / discarded |
| accuracy_feedback | enum/null | accurate / partly / wrong |
| created_at | datetime | 创建时间 |

---

## 8.4 conflict_reviews 表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | varchar | 冲突复盘 ID |
| event_id | varchar | 对应冲突事件 |
| room_id | varchar | 所属房间 |
| creator_id | varchar | 发起者 |
| user_view | text | 发起者视角 |
| partner_view | text/null | 对方视角 |
| ai_review_json | jsonb | AI 共同复盘 |
| status | enum | draft / waiting_partner / completed / archived |
| visibility | enum | private / room_visible |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

---

## 8.5 shared_decisions 表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | varchar | 决定 ID |
| room_id | varchar | 房间 ID |
| source_event_id | varchar/null | 来源事件 |
| title | varchar | 决定标题 |
| content | text | 决定内容 |
| owner_user_id | varchar/null | 负责人 |
| due_date | date/null | 截止时间 |
| status | enum | active / done / expired / archived |
| created_at | datetime | 创建时间 |

---

## 9. API 设计草案

### 9.1 记录与 AI 整理

```http
POST /api/events/draft
POST /api/ai/extract-event
POST /api/events/confirm
POST /api/events/{event_id}/regenerate-ai
PUT  /api/events/{event_id}
GET  /api/events
GET  /api/events/{event_id}
DELETE /api/events/{event_id}
```

### 9.2 冲突复盘

```http
POST /api/conflicts
GET  /api/conflicts/{conflict_id}
POST /api/conflicts/{conflict_id}/invite-partner
POST /api/conflicts/{conflict_id}/partner-view
POST /api/ai/conflict-review
PUT  /api/conflicts/{conflict_id}
```

### 9.3 房间

```http
POST /api/rooms
GET  /api/rooms
GET  /api/rooms/{room_id}
POST /api/rooms/{room_id}/invite
POST /api/rooms/join
GET  /api/rooms/{room_id}/events
GET  /api/rooms/{room_id}/reports/weekly
```

### 9.4 周期报告

```http
POST /api/ai/personal-weekly-report
POST /api/ai/room-weekly-report
POST /api/ai/monthly-pattern-analysis
GET  /api/reports
GET  /api/reports/{report_id}
```

---

## 10. 权限与隐私设计

### 10.1 可见范围

| 权限 | 说明 |
|---|---|
| private | 仅自己可见 |
| summary_only | 对方只能看到 AI 摘要或事件标题，不看原文 |
| room_visible | 房间成员可见 |

### 10.2 默认权限规则

| 事件类型 | 默认权限 |
|---|---|
| conflict | private |
| stress | private |
| health | private |
| finance | private |
| communication | summary_only |
| positive_moment | summary_only |
| decision | room_visible |
| plan | room_visible |

### 10.3 AI 使用权限

1. 个人周报可使用用户自己的 private 内容。
2. 房间周报只能使用 room_visible 和 summary_only 内容。
3. summary_only 不得向对方展示原始文本。
4. 冲突复盘如果双方参与，AI 只能使用双方明确提交给该复盘的内容。
5. 用户删除事件后，后续报告不得继续引用该事件。

---

## 11. 页面设计说明

## 11.1 首页

### 页面目标

让用户最快开始记录，而不是开始设置。

### 页面结构

```text
首页
│
├── 主输入框
│   └── 今天发生了什么？
│
├── 快捷按钮
│   ├── 冲突
│   ├── 沟通
│   ├── 开心时刻
│   ├── 压力
│   └── 决定
│
├── AI 待确认
│   └── 你有 2 条记录待确认
│
├── 最近事件
│   ├── 昨天 因见面计划产生争执
│   ├── 前天 夜班后状态较差
│   └── 本周 讨论了下月见面安排
│
└── 本周复盘
    └── AI 已整理 5 条事件，可生成周报
```

---

## 11.2 AI 整理确认页

### 页面目标

让用户用最低成本确认 AI 结果。

### 页面结构

```text
AI 整理结果
│
├── 标题，可编辑
├── 摘要，可编辑
├── 类型，可修改
├── 标签，折叠展示
├── 可见范围
├── 是否加入房间
├── 是否稍后复盘
└── 操作
    ├── 确认保存
    ├── 手动修改
    ├── 重新生成
    └── 丢弃
```

---

## 11.3 冲突复盘页

### 页面结构

```text
冲突复盘
│
├── 原始事件
├── AI 中立整理
│   ├── 事实
│   ├── 情绪
│   ├── 需求
│   └── 待确认问题
│
├── 我的补充
├── 是否邀请对方
├── AI 共同复盘
└── 下次约定
```

---

## 12. 技术方案建议

### 12.1 前端

建议：微信小程序原生开发。

理由：

- MVP 范围聚焦，原生开发足够。
- 微信邀请链路适合双人房间。
- 后续可接订阅消息做复盘提醒。

### 12.2 后端

建议技术栈：

```text
Python FastAPI
PostgreSQL
Redis
对象存储
LLM API
```

选择 FastAPI 的原因：

- 适合 AI 接口编排。
- Python 生态方便后续做文本分析、RAG、任务队列。
- 与 LLM 调用、Prompt 管理、异步任务整合成本低。

### 12.3 AI 服务层

建议拆出独立 AI Orchestrator：

```text
业务后端
↓
AI Orchestrator
├── Prompt 管理
├── 模型路由
├── JSON Schema 校验
├── 敏感内容检测
├── 结果缓存
└── 调用日志
```

### 12.4 异步任务

AI 周报、月报、模式分析建议异步生成。

```text
用户触发 / 定时任务
↓
写入任务队列
↓
AI Worker 处理
↓
结果入库
↓
通知用户查看
```

---

## 13. Sprint 计划

## Sprint 0：产品与技术准备

周期：3-5 天

交付物：

- V0.2 PRD 确认。
- AI 输出 Schema 确认。
- Prompt V1 确认。
- 数据库表设计确认。
- 小程序原型确认。

验收标准：

- 产品范围冻结。
- P0 功能明确。
- AI 调用链路 Demo 跑通。

---

## Sprint 1：快速记录与 AI 整理

周期：1-2 周

开发内容：

- 微信登录。
- 首页快速记录。
- 事件草稿创建。
- AI 事件整理接口。
- AI 整理确认页。
- 事件时间线。

验收标准：

- 用户输入一段文字后，AI 可生成结构化结果。
- 用户可确认、修改、丢弃结果。
- 确认后事件进入时间线。

---

## Sprint 2：双人房间与权限

周期：1-2 周

开发内容：

- 创建房间。
- 邀请加入。
- 房间事件时间线。
- 事件可见范围。
- summary_only 权限控制。

验收标准：

- 用户可邀请另一人加入房间。
- 私密事件不向对方展示。
- summary_only 不展示原文。

---

## Sprint 3：冲突复盘

周期：1-2 周

开发内容：

- 记录一次冲突入口。
- 冲突 AI 整理。
- 冷静复盘模板。
- 邀请对方复盘。
- AI 共同复盘。

验收标准：

- 用户可完成一次完整冲突记录。
- 用户可在冷静后补充复盘。
- 双方参与时，AI 可生成中立共同复盘。

---

## Sprint 4：周报与模式识别

周期：1-2 周

开发内容：

- 个人周报。
- 房间周报。
- 高频主题统计。
- 月度模式分析初版。
- 周报保存与分享。

验收标准：

- 用户可基于一周事件生成周报。
- 房间周报只使用授权内容。
- 系统可识别高频主题。

---

## 14. 关键指标 Metrics

### 14.1 AI 整理质量指标

| 指标 | 含义 |
|---|---|
| AI 结果确认率 | 用户直接确认的比例 |
| AI 结果编辑率 | 用户修改 AI 内容的比例 |
| 重新生成率 | 用户要求 AI 重做的比例 |
| 丢弃率 | 用户放弃 AI 结果的比例 |
| 准确反馈率 | 用户标记“准确”的比例 |

### 14.2 用户行为指标

| 指标 | 含义 |
|---|---|
| 首次记录完成率 | 用户是否完成第一条记录 |
| 每周事件数 | 用户是否形成记录习惯 |
| 周报生成率 | 是否进入复盘链路 |
| 冲突复盘完成率 | 核心场景是否成立 |
| 双人房间活跃率 | 双人协作是否成立 |

### 14.3 产品价值指标

| 指标 | 含义 |
|---|---|
| 周报保存率 | AI 总结是否有价值 |
| 共同决定生成数 | 复盘是否转化为行动 |
| 重复模式点击率 | 用户是否关注长期问题 |
| 用户主动分享率 | 用户是否认为结果值得给对方看 |

---

## 15. 关键风险与应对

### 风险 1：AI 整理不准确

应对：

- 所有 AI 结果必须人工确认。
- 保留原始输入。
- 提供“重新生成”和“手动修改”。
- 记录用户反馈，用于优化 Prompt。

### 风险 2：冲突复盘变成审判工具

应对：

- 默认私密保存。
- AI 文案不站队。
- 不输出谁对谁错。
- 双人复盘必须双方主动提交视角。
- 禁止将 private 内容用于共同复盘。

### 风险 3：隐私泄露

应对：

- 权限校验放在后端。
- AI 输入构造阶段过滤权限。
- summary_only 只展示摘要，不展示原文。
- 增加权限相关自动化测试。

### 风险 4：AI 输出越界

应对：

- Prompt 明确禁止诊断、投资建议、关系去留建议。
- 对高风险内容进行安全识别。
- 输出加入“不替代专业意见”的边界提示。
- 高风险事件不进入普通复盘流程。

### 风险 5：产品过度依赖 AI，基础体验弱

应对：

- AI 失败时允许保存原始记录。
- 事件时间线、房间、权限必须不依赖 AI 也能工作。
- AI 结果可延迟生成，不阻塞用户记录。

---

## 16. MVP 验收清单

### P0

- [ ] 用户可微信登录。
- [ ] 用户可输入一条自然语言记录。
- [ ] AI 可生成结构化事件。
- [ ] 用户可确认、编辑、重新生成、丢弃 AI 结果。
- [ ] 用户可查看个人事件时间线。
- [ ] 用户可创建双人房间。
- [ ] 用户可邀请另一人加入。
- [ ] 用户可设置 private / summary_only / room_visible。
- [ ] 用户可记录一次冲突。
- [ ] AI 可生成冲突整理结果。
- [ ] 用户可完成冷静复盘。
- [ ] AI 不向对方展示未授权原文。

### P1

- [ ] 用户可生成个人周报。
- [ ] 房间可生成共同周报。
- [ ] AI 可识别高频主题。
- [ ] 用户可生成温和沟通稿。
- [ ] 用户可沉淀共同决定。
- [ ] 系统可提醒未复盘事件。

---

## 17. 最小可用闭环

### 个人闭环

```text
快速记录
↓
AI 整理
↓
人工确认
↓
进入时间线
↓
AI 周报
↓
确定下周行动
```

### 情侣冲突闭环

```text
记录冲突
↓
AI 中立整理
↓
用户确认
↓
冷静后复盘
↓
可选邀请对方
↓
AI 共同复盘
↓
形成下次约定
```

---

## 18. 结论

本版产品的核心不再是“让用户打卡”，而是：

> 让用户用最低成本记录真实事件，再由 AI 自动整理、总结和识别长期模式。

第一版必须把资源集中在三件事上：

1. 快速记录体验是否足够轻。
2. AI 整理结果是否足够准。
3. 冲突复盘是否真的能帮助双方从情绪转向理解和行动。

其他功能都应该让位。否则产品会再次滑回“字段很多、用户很累、AI 像贴纸”的传统灾难路线。互联网墓地里这种产品已经够多了，不必再添一块碑。
